(ns repl-tasks.core-test
  (:require [clojure.test :refer :all]
            [repl-tasks.core :refer :all]))
;
; so that >lein-midje displays something

(deftest a-test
  (testing "Simple test"
    (is (= 1 1))))
